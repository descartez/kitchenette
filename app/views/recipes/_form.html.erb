<%= form_with(model: recipe, local: true, html: {class: "form"}) do |form| %>
  <% if recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.label(:title, class: "form-label") %>
  <%= form.text_field(:title, class: "text-input") %>
  <%= form.label(:ingredients, class: "form-label") %>
  <%= form.text_area(:ingredients, class: "textarea") %>
  <%= form.label(:directions, class: "form-label") %>
  <%= form.text_area(:directions, class: "textarea") %>
  <br>
  <div class="actions">
    <%= form.submit("Submit Recipe", class: "button") %>
  </div>
<% end %>
